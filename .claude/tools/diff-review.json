{
  "name": "diff_review",
  "description": "Collect review context from git changes and return structured prompt payload.",
  "command": "npm run claude:tool",
  "input_schema": {
    "type": "object",
    "properties": {
      "cwd": {
        "type": "string",
        "description": "Repository path (defaults to current working directory)."
      },
      "scope": {
        "type": "string",
        "oneOf": [
          {
            "const": "git-diff",
            "description": "Compare working tree changes against HEAD (tracked and untracked changes)."
          },
          {
            "const": "last-commit",
            "description": "Compare the last commit against its parent commit."
          },
          {
            "const": "all-files",
            "description": "Include all current text files from the working tree without diff comparison."
          }
        ],
        "description": "Which file scope to collect."
      }
    },
    "additionalProperties": false
  }
}
